#! /usr/bin/env python

from pearl_task1 import manipulator
from hlpr_manipulation_utils.manipulator import *

if __name__ == '__main__':
  rospy.init_node('circle', anonymous=True)
  arm = Arm()
  arm.circle()
